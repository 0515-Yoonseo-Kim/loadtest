name: Deploy to AWS

on:
  push:
    branches:
      - main
      - infra/*
  workflow_dispatch:  # 수동 실행 가능

jobs:
  ssh-check:
    runs-on: ubuntu-latest
    steps:
      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.5.4
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Check SSH Connection
        run: |
          ssh -o StrictHostKeyChecking=no ec2-user@${{ secrets.PUBLIC_IP }} "echo 'SSH 연결 성공!'"

  deploy:
    runs-on: ubuntu-latest
    needs: ssh-check
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.5.4
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no ec2-user@${{ secrets.PUBLIC_IP }} << 'EOF'
            set -e  # 에러 발생 시 즉시 종료

            echo "🚀 Updating system packages..."
            sudo yum update -y

            echo "📌 Checking and installing Git..."
            if ! command -v git &> /dev/null
            then
                sudo yum install -y git
            fi

            echo "📂 Checking Git Repository..."
            if [ ! -d "/home/ec2-user/app/.git" ]; then
                echo "🛠️ No Git repository found! Cloning..."
                git clone https://github.com/0515-Yoonseo-Kim/loadtest.git
            else
                echo "🔄 Pulling latest code..."
                cd /home/ec2-user/app
                git pull origin main
            fi

            echo "🐳 Checking Docker & Docker Compose..."
            if ! command -v docker &> /dev/null
            then
                echo "⚠️ Docker is not installed! Installing now..."
                sudo yum install -y docker
                sudo systemctl start docker
                sudo systemctl enable docker
            fi

            if ! command -v docker-compose &> /dev/null
            then
                echo "⚠️ Docker Compose is not installed! Installing now..."
                sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
                sudo chmod +x /usr/local/bin/docker-compose
            fi

            echo "📂 Checking docker-compose.yml..."
            if [ ! -f "/home/ec2-user/app/docker-compose.yml" ]; then
                echo "❌ Error: docker-compose.yml not found in /home/ec2-user/app!"
                exit 1
            fi

            echo "🚀 Running Docker Compose..."
            cd /home/ec2-user/app
            docker-compose down
            docker-compose pull
            docker-compose up -d

            echo "🧹 Cleaning up unused Docker resources..."
            docker system prune -af

            echo "✅ Deployment completed successfully!"
          EOF
